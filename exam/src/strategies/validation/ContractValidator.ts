import { DocumentValidator } from "./DocumentValidator";

export class ContractValidator extends DocumentValidator {
    // max 3MB in size
    // extensions: .pdf
    // requires metadata: author & version
}